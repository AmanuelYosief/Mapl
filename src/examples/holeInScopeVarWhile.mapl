// inputs: 0
// output: 256
// calculate 2 to the power 8
proc main() {
  int x;
  int y;
  int z;
  x = 8;
  y = 2;
  z = 1;
  while (0 < x) do {
    z = z * y;
    { int z; z = 1; x = x - z; }
    x = x - 1;
  }
  output z;
  outchar 13; outchar 10;
}
